services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: travelmode
      POSTGRES_PASSWORD: travelmode
      POSTGRES_DB: travelmode
    ports:
      - "5433:5432"
    volumes:
      - travelmode_postgres:/var/lib/postgresql/data

volumes:
  travelmode_postgres:
